spring.application.name=API-Error-Tracker-Analyzer

# Database connection
spring.datasource.url=jdbc:postgresql://localhost:5432/apitrackerdb
spring.datasource.username=postgres
spring.datasource.password=amitpatle
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.default_schema=api

server.port=8000

